Support the built in XML parser shipped with pike (would be much slower)

Support generation of RSS XML data.

Create RSS feeds from scratch.

Parsing of fields such as dates into Calendar objects?

Atom support? (yes, Thanks, James!)

