Version 1.3
- Fix a bug that prevents empty feeds from being generated.

Version 1.2
- Removed some xml files that were causing document extraction to fail.

Version 1.1
- Fix autodoc markup.
- Fix for running when the prerequisites have not been installed (graceful abort).

Version 1.0

- Combined existing Public.Web.RSS module with James Harton's ATOM parser.
- Added support for some additional RSS element types
- Fixed a few bugs in the ATOM parser that prevented it from compiling properly.
